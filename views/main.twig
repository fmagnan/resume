<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <title>{{ bio.firstname }} {{ bio.lastname }} -- CV</title>

    {% for cssFile in cssFiles[mode] %}
        <link rel="stylesheet" type="text/css" href="{{ cssFile }}" />
    {% endfor %}


</head>

<body>
<div id="wrapper">
    <div id="banner">
        {% include 'bio.twig' %}
        {% include 'info.twig' %}
    </div>
    {% include 'experiences.twig' %}
    {% include 'skills.twig' %}
    {% include 'education.twig' %}
</div>
</body>
</html>